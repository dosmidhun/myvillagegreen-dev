{% capture easylockdownPage404Content %}
  {% if page.metafields.accentuate.banner_image != null %}
<div class="row wrap">
  	<div class="col-xs-12"> 
        {% comment %}
      <header class="page-header">
        {% if settings.enable_multilang %}
        <h2 class="lang1">{{ page.title | split: '|' | first }}</h2>
        <h2 class="lang2">{{ page.title | split: '|' | last }}</h2>
        {% else %}
        <h2>{{ page.title | split: '|' | first }}</h2>
        {% endif %}
      </header>
      {% endcomment %}
      {% comment %}
        Regular page content goes here.
      {% endcomment %}
      <div id="shopify-section-collection-header" class="shopify-section nutrition_pag">
        <header class="page-header pharmacy">
          <div class="img_cart">
            <img src="{{ page.metafields.accentuate.banner_image }}" alt="banner image">
          </div>
          <h3> {{ page.title}}</h3>
    	</header>
     </div> 
    </div>
</div>
{% else %}

{% endif %}
<div class="row">
  	<div class="col-xs-3">
   		 <div class="widget sidebar-links sidebar-custom inner_sidebar">
            {% section 'page-sidebar' %}
      	</div>
	</div>

  	<div class="col-xs-9"> 
        {% comment %}
      <header class="page-header">
        {% if settings.enable_multilang %}
        <h2 class="lang1">{{ page.title | split: '|' | first }}</h2>
        <h2 class="lang2">{{ page.title | split: '|' | last }}</h2>
        {% else %}
        <h2>{{ page.title | split: '|' | first }}</h2>
        {% endif %}
      </header>
      {% endcomment %}
      {% comment %}
        Regular page content goes here.
      {% endcomment %}

      {% if settings.enable_multilang %}
      <div class="rte lang1">{{ page.content | split: '[lang2]' | first }}</div>
      <div class="rte lang2">{{ page.content | split: '[lang2]' | last }}</div>
      {% else %}
      <div class="rte">{{ page.content | split: '[lang2]' | first }}</div>
      {% endif %}
      </div>
</div>
<div class='easylockdown-message-404'>{{ easylockdownPage404Message }}</div>
{% endcapture %}

{% if easylockdownON %}
  <div id='easylockdown404'>{{ easylockdownPage404Content }}</div>
{% else %}
  <div class='easylockdown-content'>{{ content_for_layout }}</div>
{% endif %}

{% if customer %}
{% for tag in customer.tags %}
{% if tag == 'mvg-membership' %}
{% assign bold_member = true %}
{% break %}
{% endif %}
{% endfor %}
{% if bold_member %}


  
  {% else %}
  <script id="vest1">

jQuery(function($){
  if (window.location.pathname=="/pages/membership-dashboard"){
            window.location.href="/";
        }
        else{
         
        }
  
   
 });
</script>
  
{% endif %}
{% endif %}